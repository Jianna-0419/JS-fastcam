// Don't give up
// 포기하지 말자 💥



// 함수 선언
    function doSomething() {
        console.log('hello');
    }
    // 선언만 하면 아무것도 출력이 되지 않는다 
    // doSomething 은 기능을 하는 함수 

    function add(a, b) {
        const sum = a + b;
        return sum;     // 계산한 값을 전달
    }
    // add 는 어떤 특정한 값을 리턴하는 함수 

// 함수 호출
    doSomething();
    const result = add(1, 2);       // 전달된 값을 변수에 할당 가능
    console.log(result);

